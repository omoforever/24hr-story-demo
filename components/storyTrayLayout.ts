// Shared so the add tile and the story avatars stay the same size — if they drift apart the tray
// row visibly misaligns. DESIGN.md calls for ~56-64px at mobile width.
export const AVATAR_SIZE = 64;
